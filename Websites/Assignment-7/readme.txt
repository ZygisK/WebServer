cd server
npm install

cd client
npm install

npm start server
npm start client (after server)

server will be running on port 3000

the client will ask to autorun on 3001 because 3000 is in use already.


tested with both windows and mac, so no issues should occer.

f